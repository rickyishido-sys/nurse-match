/** Display label — unified across all screens. */
export const HANAKAI_USAGE_FEE_LABEL = 'HANAKAI利用料';

/** Row key in `hanakai_platform_settings` (future admin UI). */
export const HANAKAI_USAGE_FEE_SETTING_KEY = 'usage_fee_jpy';

/** Initial default when env / settings table are unset. */
export const DEFAULT_HANAKAI_USAGE_FEE_JPY = 500;
